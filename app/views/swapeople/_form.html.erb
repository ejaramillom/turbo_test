<%= form_with(model: swaperson) do |form| %>
  <% if swaperson.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(swaperson.errors.count, "error") %> prohibited this swaperson from being saved:</h2>

      <ul>
        <% swaperson.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
